{
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "numReplicas": 1,
    "releaseCommand": "alembic upgrade head",
    "startCommand": "alembic upgrade head && python web_server.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
